package com.merstats.vex.ui;

import javafx.fxml.FXML;

public class MainController {

    // This method runs automatically the moment the FXML file is loaded.
    // We use it to set up our initial data.
    @FXML
    public void initialize() {
        System.out.println("The Controller has successfully linked to the FXML View!");
    }
}